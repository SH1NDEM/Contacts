<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:View.Converters"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <UserControl.Resources>

        <!-- Инверсия булевого значения (False → True, True → False) -->
        <conv:NotNullToBoolConverter x:Key="BoolInvert"/>

        <!-- Стиль для всех TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate />
                </Setter.Value>
            </Setter>

            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}" />

            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="#FFFFE5E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Фото -->
        <Image 
            Grid.Column="0"
            Margin="10,42,10,0"
            Width="100" Height="100"
            Source="/Person.jpg"
            VerticalAlignment="Top"/>

        <!-- Поля -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Name:" Grid.Row="0"/>
            <TextBox Grid.Row="1"
                     Text="{Binding Name, ValidatesOnDataErrors=True}"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolInvert}}"
                     HorizontalAlignment="Stretch"/>

            <Label Content="Phone Number:" Grid.Row="2"/>
            <TextBox Grid.Row="3"
                     Text="{Binding PhoneNumber, ValidatesOnDataErrors=True}"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolInvert}}"
                     HorizontalAlignment="Stretch"/>

            <Label Content="Email:" Grid.Row="4"/>
            <TextBox Grid.Row="5"
                     Text="{Binding Email, ValidatesOnDataErrors=True}"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolInvert}}"
                     HorizontalAlignment="Stretch"/>
        </Grid>
    </Grid>
</UserControl>
