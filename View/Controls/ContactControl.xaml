<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:ViewModel"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <UserControl.Resources>

        <!-- Стиль для всех TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <!-- Убираем стандартную красную рамку -->
                <Setter.Value>
                    <ControlTemplate />
                </Setter.Value>
            </Setter>

            <!-- Подсказка -->
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}" />

            <Style.Triggers>

                <!-- Если ошибка — красная рамка -->
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="#FFFFE5E5"/>
                </Trigger>

            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid>
        <!-- Колонки: фото слева, поля справа -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"
                              MaxWidth="700"/>
        </Grid.ColumnDefinitions>

        <!-- Фото -->
        <Image 
            Grid.Column="0"
            Margin="10,42,10,0"
            Width="100" Height="100"
            Source="/Peson.jpg"
            VerticalAlignment="Top"/>

        <!-- Поля контакта -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Name:" Grid.Row="0"/>
            <TextBox Text="{Binding Name, 
                            UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True}"
                     Grid.Row="1"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolToReedConverter}}"
                     HorizontalAlignment="Stretch"/>

            <Label Content="Phone Number:" Grid.Row="2"/>
            <TextBox Text="{Binding PhoneNumber,
                                    UpdateSourceTrigger=PropertyChanged,
                                    ValidatesOnDataErrors=True}"
                     Grid.Row="3"
                     behaviors:PhoneInputBehavior.EnablePhoneFilter="True"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolToReedConverter}}"
                     HorizontalAlignment="Stretch"/>


            <Label Content="Email:" Grid.Row="4"/>
            <TextBox Text="{Binding Email, 
                                    UpdateSourceTrigger=PropertyChanged,
                                    ValidatesOnDataErrors=True}"
                     Grid.Row="5"
                     IsReadOnly="{Binding IsEditing, Converter={StaticResource BoolToReedConverter}}"
                     HorizontalAlignment="Stretch"/>
        </Grid>
    </Grid>
</UserControl>
